{
  "name": "micro-journal",
  "version": "1.0.0",
  "private": true,
  "description": "A beautiful micro-journaling app with mood tracking and streak counters",
  "scripts": {
    "setup": "npm run install:all && npm run docker:up && sleep 3 && npm run db:init",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "db:init": "cd backend && npm run db:init",
    "db:seed": "cd backend && npm run db:seed",
    "db:migrate": "cd backend && npm run migration:run",
    "db:migrate:generate": "cd backend && npm run migration:generate",
    "db:migrate:revert": "cd backend && npm run migration:revert"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
